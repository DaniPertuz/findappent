import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    gap: 5,
    marginTop: 40,
  },
  headerContainer: {
    flex: 1,
    gap: 5,
  },
  mainPhoto: {
    alignSelf: 'center',
    borderRadius: 32,
    height: 62,
    width: 62,
  },
});
