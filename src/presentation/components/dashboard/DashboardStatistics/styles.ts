import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  chartContainer: {
    gap: 20,
  },
  container: {
    flex: 1,
    gap: 40,
  },
  scrollContainer: {
    flex: 1,
  },
  scrollContentContainer: {
    gap: 40,
  },
});
