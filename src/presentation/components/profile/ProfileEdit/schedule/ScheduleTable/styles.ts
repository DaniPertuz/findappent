import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: { flex: 1, marginTop: 10 },
  rowHeight: { height: 40 },
  rowMarginStart: { marginStart: 6 },
  rowMargins: { margin: 6 },
  tableBorderWidth: { borderWidth: 1 },
});
