import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
    flexDirection: 'row',
    gap: 10,
  },
  editIcon: {
    height: 30,
    padding: 10,
    width: 30,
  },
  titleContainer: {
    maxWidth: '85%',
  },
});
