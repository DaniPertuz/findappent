import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  h1: {
    fontFamily: 'Inter_28pt-Bold',
    fontSize: 36,
    fontWeight: '600',
    letterSpacing: -0.28,
    lineHeight: 40,
  },
});
