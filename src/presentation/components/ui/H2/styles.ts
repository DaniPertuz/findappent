import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  h2: {
    fontFamily: 'Inter_28pt-Bold',
    fontSize: 28,
    fontWeight: '700',
    letterSpacing: -0.28,
    lineHeight: 32,
  },
});
