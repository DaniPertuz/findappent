import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  h4: {
    fontFamily: 'Inter_18pt-Bold',
    fontSize: 20,
    fontWeight: '700',
    letterSpacing: -0.28,
    lineHeight: 28,
  },
});
