import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  subheadline: {
    fontFamily: 'Inter_18pt-Bold',
    fontSize: 14,
    fontWeight: '700',
    letterSpacing: -0.28,
    lineHeight: 18,
  },
});
