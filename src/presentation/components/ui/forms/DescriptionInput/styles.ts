import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  inputField: {
    fontSize: 14,
    fontWeight: '500',
    height: 80,
    lineHeight: 16,
    letterSpacing: -0.24,
    textAlignVertical: 'top',
    width: '100%',
  },
});
