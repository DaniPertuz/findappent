import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  dropdown: { height: 60, flex: 1, borderWidth: 1, zIndex: 1000 },
  iconSize: { height: 30, width: 30 },
  itemIcon: { flexDirection: 'row', alignItems: 'center', gap: 15, padding: 10 },
  picture: { borderRadius: 8, height: 42, width: 42 },
});
