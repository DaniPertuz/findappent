import { StyleSheet } from 'react-native';

export const appStyles = StyleSheet.create({
  inputIcon: {
    height: 20,
    width: 20,
  },
  keyboardContainer: {
    flex: 1,
  },
  scrollContainer: {
    flexGrow: 1,
  },
});
